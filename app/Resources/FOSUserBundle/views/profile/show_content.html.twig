{% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_user_show">
    <div class="container text-center message-block">
        <p>{{ 'profile.show.username'|trans }}: {{ user.username }}<br>
            {{ 'profile.show.email'|trans }}: {{ user.email }}
        </p>
    <div>
</div>
